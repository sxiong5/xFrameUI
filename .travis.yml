language: node_js
node_js:
  - '8'

install:
  - npm install

script:
  - npm run build

after_success:
  - cd ./dist
  - git init
  - git config --global user.name "${U_NAME}"
  - git config --global user.email "${U_EMAIL}"
  - git add .
  - git commit -m "Automatically update from travis-ci"
  - git push --quiet --force  "https://${GH_TOKEN}@${GH_REF}" master:${P_BRANCH}

branches:
  only:
    - main

env:
  global:
    - GH_REF: github.com/ShihaoXiong/xFrameUI
    - GH_TOKEN: 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
    - P_BRANCH: gh-pages
    - U_EMILE: shihao.xiong@icloud.com
    - U_NAME: ShihaoXiong
